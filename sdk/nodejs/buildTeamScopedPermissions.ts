// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Builds an `OrganizationRole.permissions` descriptor that grants the supplied scopes only when the request is on the named team. Pair with `Team.name` (or any team-name string) to scope a role to a specific team. The result is directly assignable to `OrganizationRole.permissions`. To grant scopes on more than one entity in a single role, hand-roll a `PermissionDescriptorGroup` whose `entries` list pulls the output of each helper.
 */
export function buildTeamScopedPermissions(args: BuildTeamScopedPermissionsArgs, opts?: pulumi.InvokeOptions): Promise<BuildTeamScopedPermissionsResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("pulumiservice:index:buildTeamScopedPermissions", {
        "permissions": args.permissions,
        "teamName": args.teamName,
    }, opts);
}

export interface BuildTeamScopedPermissionsArgs {
    /**
     * The set of scopes to grant when the request is on the named team. Discover valid scope names via the `getOrganizationRoleScopes` data source.
     */
    permissions: string[];
    /**
     * The target team's name (e.g. `Team.name`). The Pulumi Cloud RBAC API keys team scopes by name.
     */
    teamName: string;
}

export interface BuildTeamScopedPermissionsResult {
    /**
     * A `PermissionDescriptorCondition` tree gating a `PermissionDescriptorAllow` on the named team, ready to assign to `OrganizationRole.permissions`.
     */
    readonly permissions: {[key: string]: any};
}
/**
 * Builds an `OrganizationRole.permissions` descriptor that grants the supplied scopes only when the request is on the named team. Pair with `Team.name` (or any team-name string) to scope a role to a specific team. The result is directly assignable to `OrganizationRole.permissions`. To grant scopes on more than one entity in a single role, hand-roll a `PermissionDescriptorGroup` whose `entries` list pulls the output of each helper.
 */
export function buildTeamScopedPermissionsOutput(args: BuildTeamScopedPermissionsOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<BuildTeamScopedPermissionsResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("pulumiservice:index:buildTeamScopedPermissions", {
        "permissions": args.permissions,
        "teamName": args.teamName,
    }, opts);
}

export interface BuildTeamScopedPermissionsOutputArgs {
    /**
     * The set of scopes to grant when the request is on the named team. Discover valid scope names via the `getOrganizationRoleScopes` data source.
     */
    permissions: pulumi.Input<pulumi.Input<string>[]>;
    /**
     * The target team's name (e.g. `Team.name`). The Pulumi Cloud RBAC API keys team scopes by name.
     */
    teamName: pulumi.Input<string>;
}
