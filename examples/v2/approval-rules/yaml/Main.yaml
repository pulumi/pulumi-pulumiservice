resources:
  approvers:
    type: pulumiservice:v2:PolicyGroup
    properties:
      orgName: ${serviceOrg}
      name: v2-approvers
      entityType: stacks
outputs:
  policyGroupName: ${approvers.name}
