resources:
  pool:
    type: pulumiservice:v2:AgentPool
    properties:
      orgName: ${serviceOrg}
      name: v2-agent-pool-${poolSuffix}
      description: ${poolDescription}
outputs:
  poolName: ${pool.name}
