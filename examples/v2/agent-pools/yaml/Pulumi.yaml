name: pulumicloud-v2-agent-pools
runtime: yaml
config:
  organizationName:
    type: string
    default: service-provider-test-org
  poolSuffix:
    type: string
    default: dev
  poolDescription:
    type: string
    default: v2 example agent pool
