resources:
  member:
    type: pulumiservice:v2:OrganizationMember
    properties:
      orgName: ${organizationName}
      userLogin: ${memberLogin}
      role: ${memberRole}
outputs:
  memberId: ${member.id}
