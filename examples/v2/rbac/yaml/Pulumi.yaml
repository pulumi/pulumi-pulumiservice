name: pulumicloud-v2-rbac
runtime: yaml
config:
  organizationName:
    type: string
    default: service-provider-test-org
  nameSuffix:
    type: string
    default: manual
  roleDescription:
    type: string
    default: Read-only access to stacks, created by the v2 rbac example.
