name: pulumicloud-api-insights-account
runtime: yaml
config:
  organizationName:
    type: string
    default: service-provider-test-org
  accountSuffix:
    type: string
    default: dev
  insightsEnvironment:
    type: string
    default: insights/credentials
