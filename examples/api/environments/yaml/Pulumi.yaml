name: pulumicloud-api-environments
runtime: yaml
config:
  organizationName:
    type: string
    default: service-provider-test-org
  projectName:
    type: string
    default: test-project
  envSuffix:
    type: string
    default: dev
  tagValue:
    type: string
    default: env-tag-initial
