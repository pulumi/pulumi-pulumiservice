name: pulumicloud-api-custom-vcs
runtime: yaml
config:
  organizationName:
    type: string
    default: service-provider-test-org
  vcsSuffix:
    type: string
    default: dev
  baseUrl:
    type: string
    default: https://git.example.invalid
  envRef:
    type: string
    default: organization/vcs-credentials
