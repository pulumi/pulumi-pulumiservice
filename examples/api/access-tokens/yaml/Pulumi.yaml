name: pulumicloud-api-access-tokens
runtime: yaml
config:
  organizationName:
    type: string
    default: service-provider-test-org
  tokenSuffix:
    type: string
    default: dev
  tokenDescription:
    type: string
    default: example api access token
