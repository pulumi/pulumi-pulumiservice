resources:
  parentStack:
    type: pulumiservice:v1/stacks:Stack
    properties:
      orgName: ${organizationName}
      projectName: ${projectName}
      stackName: ${stackName}
  ownerTag:
    type: pulumiservice:v1/stacks:Tag
    properties:
      orgName: ${organizationName}
      projectName: ${parentStack.projectName}
      stackName: ${parentStack.stackName}
      name: owner
      value: pulumicloud-v1-example
  customTag:
    type: pulumiservice:v1/stacks:Tag
    properties:
      orgName: ${organizationName}
      projectName: ${parentStack.projectName}
      stackName: ${parentStack.stackName}
      name: purpose
      value: ${tagValue}
outputs:
  parent: ${parentStack.id}
