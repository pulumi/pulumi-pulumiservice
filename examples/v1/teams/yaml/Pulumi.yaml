name: pulumicloud-v1-teams
runtime: yaml
config:
  organizationName:
    type: string
    default: service-provider-test-org
  teamSuffix:
    type: string
    default: dev
  teamDescription:
    type: string
    default: A team created by the v1 example.
