name: pulumicloud-v1-webhooks
runtime: yaml
config:
  organizationName:
    type: string
    default: service-provider-test-org
  secretValue:
    type: string
    default: shhh
  hookSuffix:
    type: string
    default: dev
