name: pulumicloud-v1-task
runtime: yaml
config:
  organizationName:
    type: string
    default: service-provider-test-org
  taskSuffix:
    type: string
    default: dev
  taskID:
    type: string
    default: example-task-id
