name: pulumiservice-canonical-04-py
runtime: python
description: Deployment-pipeline scenario, Python variant.
config:
  organizationName:
    type: string
    default: service-provider-test-org
  project:
    type: string
    default: infrastructure
  stack:
    type: string
    default: production
