name: pulumiservice-canonical-07-cs
runtime: dotnet
description: Tiered-team-access scenario, .NET (C#) variant.
config:
  organizationName:
    type: string
    default: service-provider-test-org
  digits:
    type: string
    default: "00000"
