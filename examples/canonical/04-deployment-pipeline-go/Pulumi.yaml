name: pulumiservice-canonical-04-go
runtime: go
description: Deployment-pipeline scenario, Go variant.
config:
  organizationName:
    type: string
    default: service-provider-test-org
  project:
    type: string
    default: infrastructure
  stack:
    type: string
    default: production
