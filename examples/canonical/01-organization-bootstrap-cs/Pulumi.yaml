name: pulumiservice-canonical-01-cs
runtime: dotnet
description: Organization-bootstrap scenario, .NET (C#) variant.
config:
  organizationName:
    type: string
    default: service-provider-test-org
  digits:
    type: string
    default: "00000"
