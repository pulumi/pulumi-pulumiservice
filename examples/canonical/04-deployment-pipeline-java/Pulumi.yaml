name: pulumiservice-canonical-04-java
runtime: java
description: Deployment-pipeline scenario, Java variant.
config:
  organizationName:
    type: string
    default: service-provider-test-org
  project:
    type: string
    default: infrastructure
  stack:
    type: string
    default: production
