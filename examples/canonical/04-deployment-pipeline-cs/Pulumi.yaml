name: pulumiservice-canonical-04-cs
runtime: dotnet
description: Deployment-pipeline scenario, .NET (C#) variant.
config:
  organizationName:
    type: string
    default: service-provider-test-org
  project:
    type: string
    default: infrastructure
  stack:
    type: string
    default: production
