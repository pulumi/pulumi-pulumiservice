# Deployment Settings with VCS Configuration Example (YAML)

This example configures deployment settings with VCS (Version Control System) integration for Azure DevOps.

To convert this example to other programming languages, see [pulumi convert](https://www.pulumi.com/docs/iac/cli/commands/pulumi_convert/).
