name: yaml-rbac-import-target
runtime: yaml
description: Empty fixture used by TestYamlRbacComposeImport as the destination of `pulumi import`. The test creates a PermissionDescriptorCompose role out-of-band, then imports it here to verify the translator yields the expected `kind` shape.
